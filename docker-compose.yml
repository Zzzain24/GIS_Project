version: '3.8'
services:
  citibike-app:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    command: >
      sh -c "python scripts/pipeline.py && streamlit run scripts/app.py --server.port=8501 --server.address=0.0.0.0"